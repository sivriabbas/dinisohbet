<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kıble Pusulası - DiniSohbet</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/new-styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="main-content">
        <div class="container">
            <div class="qibla-container">
                <div class="qibla-header">
                    <div class="qibla-icon">
                        <i class="fas fa-kaaba"></i>
                    </div>
                    <h1>Kıble Pusulası</h1>
                    <p class="qibla-subtitle">Namaz için doğru yönü bul</p>
                </div>

                <!-- Konum İzni Uyarısı -->
                <div id="location-prompt" class="location-prompt">
                    <div class="prompt-icon">
                        <i class="fas fa-location-arrow"></i>
                    </div>
                    <h3>Konumunuza İhtiyacımız Var</h3>
                    <p>Kıble yönünü hesaplamak için konumunuza erişmemiz gerekiyor.</p>
                    <button id="request-location-btn" class="btn btn-primary">
                        <i class="fas fa-map-marker-alt"></i> Konumu Paylaş
                    </button>
                </div>

                <!-- Yükleniyor -->
                <div id="loading-state" class="loading-state" style="display: none;">
                    <i class="fas fa-spinner fa-spin"></i>
                    <p>Konumunuz alınıyor...</p>
                </div>

                <!-- Ana Pusula -->
                <div id="compass-container" class="compass-container" style="display: none;">
                    <div class="compass-wrapper">
                        <!-- Pusula Arka Plan -->
                        <div id="compass-rose" class="compass-rose">
                            <div class="compass-directions">
                                <div class="direction north">K</div>
                                <div class="direction east">D</div>
                                <div class="direction south">G</div>
                                <div class="direction west">B</div>
                            </div>
                            <div class="compass-degree-marks">
                                <% for(let i = 0; i < 360; i += 10) { %>
                                    <div class="degree-mark" style="transform: rotate(<%= i %>deg);">
                                        <div class="mark <%= i % 30 === 0 ? 'major' : 'minor' %>"></div>
                                    </div>
                                <% } %>
                            </div>
                        </div>

                        <!-- Kıble İşaretçisi -->
                        <div id="qibla-arrow" class="qibla-arrow">
                            <div class="arrow-body">
                                <i class="fas fa-kaaba"></i>
                                <div class="arrow-tip"></div>
                            </div>
                            <div class="arrow-label">KABE</div>
                        </div>

                        <!-- Cihaz Yönü İşaretçisi -->
                        <div class="device-arrow">
                            <i class="fas fa-mobile-alt"></i>
                        </div>
                    </div>

                    <!-- Kıble Bilgileri -->
                    <div class="qibla-info">
                        <div class="info-card">
                            <div class="info-icon">
                                <i class="fas fa-compass"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Kıble Açısı</div>
                                <div id="qibla-angle" class="info-value">---°</div>
                            </div>
                        </div>
                        <div class="info-card">
                            <div class="info-icon">
                                <i class="fas fa-location-dot"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Konumunuz</div>
                                <div id="user-location" class="info-value">Alınıyor...</div>
                            </div>
                        </div>
                        <div class="info-card">
                            <div class="info-icon">
                                <i class="fas fa-route"></i>
                            </div>
                            <div class="info-content">
                                <div class="info-label">Mesafe (Kabe)</div>
                                <div id="distance-to-kaaba" class="info-value">--- km</div>
                            </div>
                        </div>
                    </div>

                    <!-- Cihaz Yönü Desteği -->
                    <div id="orientation-warning" class="alert alert-warning" style="display: none;">
                        <i class="fas fa-exclamation-triangle"></i>
                        <p>Pusula çalışması için telefonunuzu yatay tutun ve etrafınızda bir tur atın (kalibre için).</p>
                    </div>
                </div>

                <!-- Harita -->
                <div id="map-section" class="map-section" style="display: none;">
                    <h2 class="section-title">
                        <i class="fas fa-map"></i> Harita Görünümü
                    </h2>
                    <div id="qibla-map" class="qibla-map"></div>
                    <div class="map-legend">
                        <div class="legend-item">
                            <i class="fas fa-map-pin" style="color: #dc3545;"></i>
                            <span>Konumunuz</span>
                        </div>
                        <div class="legend-item">
                            <i class="fas fa-kaaba" style="color: #28a745;"></i>
                            <span>Kabe (Mekke)</span>
                        </div>
                    </div>
                </div>

                <!-- Hata Mesajı -->
                <div id="error-message" class="error-message" style="display: none;">
                    <i class="fas fa-exclamation-circle"></i>
                    <h3>Hata Oluştu</h3>
                    <p id="error-text"></p>
                    <button id="retry-btn" class="btn btn-secondary">
                        <i class="fas fa-redo"></i> Tekrar Dene
                    </button>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="/js/qibla.js"></script>

    <style>
        .qibla-container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 1rem;
        }

        .qibla-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .qibla-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
        }

        .qibla-icon i {
            font-size: 2.5rem;
            color: white;
        }

        .qibla-header h1 {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .qibla-subtitle {
            color: var(--text-muted);
            font-size: 1.1rem;
        }

        /* Location Prompt */
        .location-prompt {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 3rem;
            text-align: center;
            box-shadow: 0 4px 20px var(--shadow);
        }

        .prompt-icon {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }

        .prompt-icon i {
            font-size: 3rem;
            color: white;
        }

        .location-prompt h3 {
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: var(--text-color);
        }

        .location-prompt p {
            color: var(--text-muted);
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        /* Loading State */
        .loading-state {
            text-align: center;
            padding: 4rem;
        }

        .loading-state i {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .loading-state p {
            font-size: 1.2rem;
            color: var(--text-muted);
        }

        /* Compass Container */
        .compass-container {
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .compass-wrapper {
            position: relative;
            width: 400px;
            height: 400px;
            margin: 2rem auto;
        }

        /* Compass Rose */
        .compass-rose {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: radial-gradient(circle, var(--card-bg) 0%, var(--bg-secondary) 100%);
            box-shadow: 
                0 0 0 8px var(--card-bg),
                0 0 0 10px var(--border-color),
                0 15px 40px rgba(0, 0, 0, 0.2);
            transition: transform 0.1s ease-out;
        }

        .compass-directions {
            position: absolute;
            width: 100%;
            height: 100%;
        }

        .direction {
            position: absolute;
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .direction.north {
            top: 10px;
            left: 50%;
            transform: translateX(-50%);
            color: #dc3545;
        }

        .direction.east {
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
        }

        .direction.south {
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
        }

        .direction.west {
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
        }

        .compass-degree-marks {
            position: relative;
            width: 100%;
            height: 100%;
        }

        .degree-mark {
            position: absolute;
            top: 0;
            left: 50%;
            width: 2px;
            height: 100%;
            transform-origin: center;
        }

        .mark {
            position: absolute;
            top: 15px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--text-muted);
        }

        .mark.major {
            width: 3px;
            height: 15px;
        }

        .mark.minor {
            width: 2px;
            height: 8px;
        }

        /* Qibla Arrow */
        .qibla-arrow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 100%;
            height: 100%;
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .arrow-body {
            position: absolute;
            top: 10%;
            left: 50%;
            transform: translateX(-50%);
            text-align: center;
        }

        .arrow-body i {
            font-size: 2.5rem;
            color: var(--secondary-color);
            margin-bottom: 0.5rem;
            display: block;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        .arrow-tip {
            width: 0;
            height: 0;
            border-left: 15px solid transparent;
            border-right: 15px solid transparent;
            border-top: 120px solid var(--secondary-color);
            margin: 0 auto;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
        }

        .arrow-label {
            position: absolute;
            top: 5%;
            left: 50%;
            transform: translateX(-50%);
            background: var(--secondary-color);
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 12px;
            font-weight: 700;
            font-size: 0.8rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
        }

        .device-arrow {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 2rem;
            color: #3b82f6;
        }

        /* Qibla Info */
        .qibla-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin: 3rem 0;
        }

        .info-card {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            box-shadow: 0 4px 12px var(--shadow);
            transition: transform 0.3s ease;
        }

        .info-card:hover {
            transform: translateY(-5px);
        }

        .info-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .info-icon i {
            font-size: 1.8rem;
            color: white;
        }

        .info-content {
            flex: 1;
        }

        .info-label {
            font-size: 0.85rem;
            color: var(--text-muted);
            margin-bottom: 0.3rem;
        }

        .info-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        /* Map Section */
        .map-section {
            margin-top: 3rem;
        }

        .section-title {
            font-size: 1.8rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .qibla-map {
            height: 500px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 8px 24px var(--shadow);
        }

        .map-legend {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 1rem;
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1rem;
            color: var(--text-color);
        }

        .legend-item i {
            font-size: 1.2rem;
        }

        /* Alerts */
        .alert {
            padding: 1rem 1.5rem;
            border-radius: 8px;
            display: flex;
            align-items: center;
            gap: 1rem;
            margin: 1rem 0;
        }

        .alert-warning {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            color: #856404;
        }

        .alert i {
            font-size: 1.5rem;
        }

        /* Error Message */
        .error-message {
            text-align: center;
            padding: 3rem;
            background: var(--card-bg);
            border-radius: 15px;
            box-shadow: 0 4px 20px var(--shadow);
        }

        .error-message i {
            font-size: 4rem;
            color: #dc3545;
            margin-bottom: 1rem;
        }

        .error-message h3 {
            color: var(--text-color);
            margin-bottom: 1rem;
        }

        .error-message p {
            color: var(--text-muted);
            margin-bottom: 2rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .compass-wrapper {
                width: 320px;
                height: 320px;
            }

            .qibla-header h1 {
                font-size: 2rem;
            }

            .qibla-info {
                grid-template-columns: 1fr;
            }

            .qibla-map {
                height: 350px;
            }
        }
    </style>
</body>
</html>

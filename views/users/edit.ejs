<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profili Düzenle - Dini Sohbet</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <main class="main-content">
        <div class="container">
            <div class="form-container">
                <h1>Profili Düzenle</h1>
                
                <% if (error) { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                <% } %>

                <form action="/users/<%= user.username %>/edit" method="POST" class="profile-form">
                    <div class="form-group">
                        <label for="username">Kullanıcı Adı</label>
                        <input type="text" id="username" value="<%= user.username %>" disabled>
                        <small>Kullanıcı adı değiştirilemez</small>
                    </div>

                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" value="<%= user.email %>" disabled>
                        <small>Email değiştirilemez</small>
                    </div>

                    <div class="form-group">
                        <label for="bio">Biyografi</label>
                        <textarea id="bio" name="bio" maxlength="500" rows="5"><%= user.bio || '' %></textarea>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Kaydet</button>
                        <a href="/users/<%= user.username %>" class="btn btn-secondary">İptal</a>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
